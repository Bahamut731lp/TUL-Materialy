**Vlákno** je [[Proces|podproces]], který s hlavním procesem sdílí paměť dat.

Vlákna potřebují následující funkce:
- Vytvoření a spuštění (`start`)
- Synchronizace (`join`)
- Spánek (`sleep`)
- Přerušení (`interrupt`)